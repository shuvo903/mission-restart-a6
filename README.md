// 1. What is the difference between null and undefined?

// undefined means a variable has been declared but no value has been assigned yet.
// Intentionally empty or no value given is called null.

// 2. What is the use of the map() function in JavaScript? How is it different from forEach()?

// map() creates a new array, which operates on each element of the old array, creating a new array with the new values.
// forEach() only operates on each element of the array, but does not create a new array.

// 3. What is the difference between == and ===?

// == means just checking if the values ​​match.
// === means it checks whether both the value and the type match.

// 4. What is the significance of async/await in fetching API data?

// async/await is used when fetching data from API.


// 5. Explain the concept of Scope in JavaScript (Global, Function, Block).

// Scope means where a variable can be used.

// There are 3 types of Scope in JavaScript:

// 1. Global Scope
// A variable that can be used from anywhere in the entire program.

// 2. Function Scope
// A variable that can only be used inside that function.

// 3. Block Scope
// A variable that can only be used inside { } (like if, loop etc.). It cannot be used outside.